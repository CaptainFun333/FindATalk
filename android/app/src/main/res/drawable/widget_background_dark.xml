<?xml version="1.0" encoding="utf-8"?>
<!-- Explicit-override counterpart of widget_background.xml, for "Dark".
     See widget_background_light.xml for why this uses literal hex values
     instead of @color/ references. Must match values-night/colors.xml's
     widget_paper_raised/widget_line. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#1F2635" />
    <corners android:radius="16dp" />
    <stroke android:width="1dp" android:color="#333C4F" />
</shape>
